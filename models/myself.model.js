const mongoose = require('mongoose');

const Myself = mongoose.model(
    'Myself',
    new mongoose.Schema({
        // evaluate the atypique archetype according to Carl Gustav Jung's theory of psychological types and the collective unconscious
        // to create fields that will be used to evaluate the atypique archetype according tests and metadata fetched from the database
        
        
    }),
);

module.exports = Myself;
